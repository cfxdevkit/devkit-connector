{
  "name": "@conflux-wallet/contracts",
  "version": "0.1.0",
  "description": "Smart contracts for Conflux dual wallet system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "install:all": "npm run install:espace && npm run install:core",
    "install:espace": "cd espace && npm install",
    "install:core": "cd core && npm install",
    "compile:all": "npm run compile:espace && npm run compile:core",
    "compile:espace": "cd espace && npm run compile",
    "compile:core": "cd core && npm run compile",
    "test:all": "npm run test:espace && npm run test:core",
    "test:espace": "cd espace && npm test",
    "test:core": "cd core && npm test",
    "deploy:all": "npm run deploy:espace && npm run deploy:core",
    "deploy:espace": "cd espace && npm run deploy:all",
    "deploy:core": "cd core && npm run deploy:all",
    "verify:all": "npm run verify:espace && npm run verify:core",
    "verify:espace": "cd espace && npm run verify:all",
    "verify:core": "cd core && npm run verify:all",
    "clean:all": "npm run clean:espace && npm run clean:core",
    "clean:espace": "cd espace && npm run clean",
    "clean:core": "cd core && npm run clean",
    "gas-report:all": "npm run gas-report:espace && npm run gas-report:core",
    "gas-report:espace": "cd espace && npm run gas-report",
    "gas-report:core": "cd core && npm run gas-report"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "conflux",
    "wallet",
    "delegation",
    "smart-contracts",
    "espace",
    "core"
  ],
  "author": "Conflux Dual Wallet Team",
  "license": "MIT"
}
